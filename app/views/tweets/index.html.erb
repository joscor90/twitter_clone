<h1>Welcome <%= current_user ? current_user.name : "stranger" %></h1>
<p>This is my Twitter clone </p>

<% if user_signed_in? %>
    <h3>Wanna tweet something?</h3>

    <%= render 'form', tweet: @tweet %>
<% end %>

<h3>Latest tweets</h3>
<table>
    <thead>
        <tr>
            <th>Content</th>
            <th>Author</th>
            <th>Tweeted At</th>
            <th>Retweets</th>
            <th>Likes</th>
            <th colspan='4'></th>
        </tr>
    </thead>
    <tbody>
    <% @tweets.each do |tweet| %>
        <tr>
            <td><%= tweet.content %></td>
            <td><%= tweet.user %></td>
            <td><%= link_to tweet.created_at, tweet_path(tweet) %></td>
            <td><%= link_to tweet.retweets.count, retweets_path(tweet) %></td>
            <td><%= link_to tweet.likes.count %></td>
            <% if user_signed_in? %>
                <td><%= link_to 'Retweet', retweet_path(tweet) %></td>
                <% if Like.find(user_id: current_user.id, tweet_id: tweet.id) %>
                <td><%= link_to 'Destroy', create_like_path(tweet) %></td>
                <% else %>
                    <td><%= link_to 'Like', create_like_path(tweet) %></td>
                <% end %>
            <% end %>
            <% if current_user && current_user.id == tweet.user_id %>
                <td><%= link_to 'Edit', edit_tweet_path(tweet) %></td>
                <td><%= link_to 'Delete', tweet, method: :delete, data: { confirm: 'Are you sure you want to delete your tweet?' }%></td>
            <% end %>
        </tr>
    <% end %>
    </tbody>
</table>

<%= paginate @tweets %>