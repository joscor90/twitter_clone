<div class = "container">
        <h3>Share something</h3>
        <%= form_with(model: tweet, local: true) do |ff| %>
            <div class = "form-group">
                <div>
                    <%= ff.text_area :content, class: "form-control w-50" %>
                </div>
            </div>
        
            <div>
                <%= ff.submit nil, class: "btn btn-primary mt-2 mb-5" %>
            </div>
        <% end %>
</div>

